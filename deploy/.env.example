# Published host ports. Change these if something on your machine already owns
# the defaults -- 8080 and 9090 in particular are commonly taken.
#
#   cp deploy/.env.example deploy/.env
#
GATEWAY_PORT=8080
METRICS_PORT=9090
UPSTREAM_PORT=8081
PROMETHEUS_PORT=9091
REDIS_PORT=6379
